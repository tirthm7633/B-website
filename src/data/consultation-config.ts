// TODO: replace with the real Gmail (or business) address consultation
// request emails should land in. Everything downstream (the /api/consultation
// serverless function) reads this one constant, so updating it here is
// enough — no other file needs to change.
export const CONSULTATION_EMAIL = 'consultations@buildconhouse.com'
